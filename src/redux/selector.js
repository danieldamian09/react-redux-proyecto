// creamos como accesos directos a estas piezas de informacion
export const isAuthSel = state => state.loginReducer.isAuth;

export const isChekingAuthSel = state => state.loginReducer.isCheckingAuth;